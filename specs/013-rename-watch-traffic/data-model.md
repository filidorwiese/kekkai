# Data Model: Rename `kekkai watch` to `kekkai traffic`

No entities. Pure rename — no data, state, or persistence is introduced or
changed. The feature 010 in-memory session state (hostname cache,
repeat-suppression window) is untouched; see
[specs/010-inspect-egress/data-model.md](../010-inspect-egress/data-model.md).
